@model WebApp.ViewModels.BikeViewModel

@{
}
<div>
    @using (Html.BeginForm())
    {
        <div class="col-md-10">
            @Html.EditorFor(model => model.RentPointAdress, new { htmlAttributes = new { @class = "form-control" } })
        </div>

        <input type="submit" value="Create" class="btn btn-default" />
    }

</div>